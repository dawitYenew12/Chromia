entity task {
    key mutable id: byte_array;
    index user_id: byte_array;
    mutable title: text;
    mutable description: text;
    mutable due_date: integer;
    mutable status: text;
}

entity user {
  mutable name;
  key id: byte_array;
  key account;
}